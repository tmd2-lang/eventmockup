# v0 canon archive

Spreadsheets, home content JSON, import scripts, and full Supabase migrations for the **complete demo** (v0).

The frozen git tag **`v0-demo-mockup`** is the authoritative snapshot of this data in the repo history.

The active **`v1`** branch uses `supabase/migrations/001_v1_profiles_only.sql` and `npm run import:profiles` only.
